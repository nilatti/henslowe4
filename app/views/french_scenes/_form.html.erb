<%= semantic_form_for [@scene, @french_scene] do |f| %>
      <%= render 'shared_partials/french_scene_fields', f: f %>
	  <%= f.actions do %>
	    <%= f.action :submit, :as => :input %>
	  <% end %>
<% end %>
