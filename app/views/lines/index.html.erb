<p id="notice"><%= notice %></p>

<h1>Listing Lines</h1>

<table>
  <thead>
    <tr>
      <th>Text</th>
      <th>Belongs to</th>
      <th>Belongs to</th>
      <th>Type</th>
      <th>Cut</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lines.each do |line| %>
      <tr>
        <td><%= line.text %></td>
        <td><%= line.belongs_to %></td>
        <td><%= line.belongs_to %></td>
        <td><%= line.type %></td>
        <td><%= line.cut %></td>
        <td><%= link_to 'Show', line %></td>
        <td><%= link_to 'Edit', edit_line_path(line) %></td>
        <td><%= link_to 'Destroy', line, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Line', new_line_path %>
